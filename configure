WORKDIR=$PWD
BUILD=~/Dropbox/Projects/Apeiron/build
if [ ! -d $BUILD ] 
then
  echo "Directory $BUILD does not exists." 
  exit 9999
else
  echo "Configuring CMake files..."
  cd $BUILD
  rm -rf $BUILD/*
  cmake ..
  cd $WORKDIR
fi
